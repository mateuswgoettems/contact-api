# service.yaml
apiVersion: v1
kind: Service
metadata:
  name: contact-api-svc
  namespace: manutencao-de-piscina
spec:
  selector:
    app: contact-api
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP
